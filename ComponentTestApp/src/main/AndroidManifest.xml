<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.syapp.componentbasicapp">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:name=".app.ComponentTestApplication"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.syapp.componentbasicapp.testcomponent.TestComponentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".structure.StructureActivity" />
        <activity android:name="com.syapp.componentbasicapp.testcomponent.TransparentActivity" android:theme="@style/Theme.Transparent"/>
    </application>

</manifest>